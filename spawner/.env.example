# Spawner API config

PORT=8080
BIND_HOST=127.0.0.1

# Docker image to run
CLI_RUNNER_IMAGE=cli-runner:latest

# Allow-list roots (absolute paths recommended)
# Multiple roots separated by commas.
ALLOW_WORKSPACE_ROOTS=./cli-runner/volumes/workspace
ALLOW_CREDS_ROOTS=./cli-runner/volumes/creds

