# Copy to .env and set any keys you want passed to the container.

# Engine default (optional): codex | gemini | opencode
# ENGINE=codex

# Docker image name tag (optional)
# CLI_RUNNER_IMAGE=cli-runner:latest

# Provider/API keys (optional if you use OAuth flows)
# OpenAI (used by Codex and OpenCode)
# OPENAI_API_KEY=sk-...

# Gemini (AI Studio):
# GEMINI_API_KEY=...

# Gemini via Vertex AI:
# GOOGLE_GENAI_USE_VERTEXAI=true
# GOOGLE_API_KEY=...
# or use ADC via mounted gcloud folder (/home/agent/.creds/gcloud)
# GOOGLE_APPLICATION_CREDENTIALS=/home/agent/.creds/gcloud/sa.json

# Anthropic (OpenCode can use this)
# ANTHROPIC_API_KEY=...

# Extra flags to add to docker run (optional)
# EXTRA_DOCKER_RUN_ARGS="--pull=always"
